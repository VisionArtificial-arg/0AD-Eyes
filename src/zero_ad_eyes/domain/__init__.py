"""Domain core: framework-free value objects that form the integration seam.

Nothing in this package performs I/O or imports capture / CV / model frameworks.
Every adapter in ``infrastructure`` and every port in ``application`` is expressed
in terms of these types.
"""
